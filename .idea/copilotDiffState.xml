<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/app/(seller)/seller/shops/[shopId]/members/action.ts">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/app/(seller)/seller/shops/[shopId]/members/action.ts" />
              <option name="originalContent" value="import { requireSeller } from '@/lib/require-role';&#10;import { prisma } from '@/lib/db';&#10;import { randomBytes } from 'node:crypto';&#10;import { ShopMemberRole } from '@/lib/generated/prisma';&#10;import { env } from '@/lib/env';&#10;import { paths } from '@/lib/path';&#10;import { sendShopInvitationEmail } from '@/lib/mailer';&#10;import { InviteMemberSchema } from '@/app/(seller)/seller/shops/[shopId]/members/schema';&#10;import { ActionResponse } from '@/lib/service-response';&#10;&#10;export async function inviteMember(&#10;  email: string,&#10;  shopId: string,&#10;  role?: string&#10;) {&#10;  // Validate input with Zod and return structured validation errors when invalid&#10;  const parsed = InviteMemberSchema.safeParse({ email, shopId, role });&#10;  if (!parsed.success) {&#10;    const errors = parsed.error.flatten().fieldErrors;&#10;    return ActionResponse.toNextResponse(&#10;      ActionResponse.error('Validation failed', 400, errors)&#10;    );&#10;  }&#10;&#10;  const session = await requireSeller();&#10;  if (!session) {&#10;    return ActionResponse.toNextResponse(&#10;      ActionResponse.error('Unauthorized', 401)&#10;    );&#10;  }&#10;&#10;  const shop = await prisma.shop.findFirst({&#10;    where: { id: shopId, ownerId: session.user.id },&#10;  });&#10;&#10;  if (!shop) {&#10;    return ActionResponse.toNextResponse(&#10;      ActionResponse.error('Shop not found or permission denied', 403)&#10;    );&#10;  }&#10;&#10;  const existingMember = await prisma.shopMember.findFirst({&#10;    where: {&#10;      shopId,&#10;      user: { email },&#10;    },&#10;  });&#10;  if (existingMember) {&#10;    return ActionResponse.toNextResponse(&#10;      ActionResponse.error('User is already a member of this shop', 400)&#10;    );&#10;  }&#10;&#10;  try {&#10;    const token = randomBytes(32).toString('hex');&#10;    const expiresAt = new Date(Date.now() + 1000 * 60 * 60 * 24 * 3); // 3 days&#10;&#10;    await prisma.shopInvitation.upsert({&#10;      where: {&#10;        shopId_email: { shopId, email },&#10;      },&#10;      update: { token, expiresAt },&#10;      create: {&#10;        email,&#10;        shopId,&#10;        token,&#10;        expiresAt,&#10;        role: ShopMemberRole.STAFF,&#10;      },&#10;    });&#10;&#10;    const invitationLink = `${env.NEXT_PUBLIC_BASE_URL}/${paths.shop.accept_invite(&#10;      token&#10;    )}?email=${encodeURIComponent(email)}`;&#10;    await sendShopInvitationEmail(&#10;      email,&#10;      shop.name,&#10;      session.user.name || session.user.email || 'Shop Owner',&#10;      invitationLink&#10;    );&#10;&#10;    return ActionResponse.toNextResponse(&#10;      ActionResponse.success(null, 'Invitation sent successfully', 200)&#10;    );&#10;  } catch (err) {&#10;    console.error('inviteMember error', err);&#10;    return ActionResponse.toNextResponse(&#10;      ActionResponse.error('Failed to send invitation', 500)&#10;    );&#10;  }&#10;}&#10;" />
              <option name="updatedContent" value="import { requireSeller } from '@/lib/require-role';&#10;import { prisma } from '@/lib/db';&#10;import { randomBytes } from 'node:crypto';&#10;import { ShopMemberRole } from '@/lib/generated/prisma';&#10;import { env } from '@/lib/env';&#10;import { paths } from '@/lib/path';&#10;import { sendShopInvitationEmail } from '@/lib/mailer';&#10;import { InviteMemberSchema } from '@/app/(seller)/seller/shops/[shopId]/members/schema';&#10;import { ActionResponse } from '@/lib/service-response';&#10;&#10;export async function inviteMember(&#10;  email: string,&#10;  shopId: string,&#10;  role?: string&#10;) {&#10;  // Validate input with Zod and return structured validation errors when invalid&#10;  const parsed = InviteMemberSchema.safeParse({ email, shopId, role });&#10;  if (!parsed.success) {&#10;    const errors = parsed.error.flatten().fieldErrors;&#10;    return ActionResponse.toNextResponse(&#10;      ActionResponse.error('Validation failed', 400, errors)&#10;    );&#10;  }&#10;&#10;  const session = await requireSeller();&#10;  if (!session) {&#10;    return ActionResponse.toNextResponse(&#10;      ActionResponse.error('Unauthorized', 401)&#10;    );&#10;  }&#10;&#10;  const shop = await prisma.shop.findFirst({&#10;    where: { id: shopId, ownerId: session.user.id },&#10;  });&#10;&#10;  if (!shop) {&#10;    return ActionResponse.toNextResponse(&#10;      ActionResponse.error('Shop not found or permission denied', 403)&#10;    );&#10;  }&#10;&#10;  const existingMember = await prisma.shopMember.findFirst({&#10;    where: {&#10;      shopId,&#10;      user: { email },&#10;    },&#10;  });&#10;  if (existingMember) {&#10;    return ActionResponse.toNextResponse(&#10;      ActionResponse.error('User is already a member of this shop', 400)&#10;    );&#10;  }&#10;&#10;  try {&#10;    const token = randomBytes(32).toString('hex');&#10;    const expiresAt = new Date(Date.now() + 1000 * 60 * 60 * 24 * 3); // 3 days&#10;&#10;    await prisma.shopInvitation.upsert({&#10;      where: {&#10;        shopId_email: { shopId, email },&#10;      },&#10;      update: { token, expiresAt },&#10;      create: {&#10;        email,&#10;        shopId,&#10;        token,&#10;        expiresAt,&#10;        role: ShopMemberRole.STAFF,&#10;      },&#10;    });&#10;&#10;    const invitationLink = `${env.NEXT_PUBLIC_BASE_URL}/${paths.shop.accept_invite(&#10;      token&#10;    )}?email=${encodeURIComponent(email)}`;&#10;    await sendShopInvitationEmail(&#10;      email,&#10;      shop.name,&#10;      session.user.name || session.user.email || 'Shop Owner',&#10;      invitationLink&#10;    );&#10;&#10;    return ActionResponse.toNextResponse(&#10;      ActionResponse.success(null, 'Invitation sent successfully', 200)&#10;    );&#10;  } catch (err) {&#10;    console.error('inviteMember error', err);&#10;    return ActionResponse.toNextResponse(&#10;      ActionResponse.error('Failed to send invitation', 500)&#10;    );&#10;  }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>